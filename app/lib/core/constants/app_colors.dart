import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette — medium blue (lighter, friendlier)
  static const primary = Color(0xFF2962FF);
  static const primaryLight = Color(0xFF5C8AFF);
  static const primaryDark = Color(0xFF0039CB);

  // Secondary / accent
  static const secondary = Color(0xFFFF6D00);
  static const secondaryLight = Color(0xFFFF9E40);

  // Accent greens
  static const success = Color(0xFF00E676);
  static const successDark = Color(0xFF00C853);

  // Status colors
  static const live = Color(0xFF00E676);
  static const pending = Color(0xFFBDBDBD);
  static const lost = Color(0xFFEF5350);
  static const won = Color(0xFF00E676);

  // Neutrals — lighter blueish background
  static const background = Color(0xFFE8EEF6);
  static const surface = Color(0xFFF0F4FA);
  static const surfaceDark = Color(0xFF1C1C1E);
  static const backgroundDark = Color(0xFF0A0E1A);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const textOnDark = Color(0xFFE0E0E0);
  static const divider = Color(0xFFD0D8E8);

  // Token gold
  static const tokenGold = Color(0xFFFFD700);
  static const tokenGoldDark = Color(0xFFFFC107);

  // Podium
  static const gold = Color(0xFFFFD700);
  static const silver = Color(0xFFC0C0C0);
  static const bronze = Color(0xFFCD7F32);

  // Bracket
  static const bracketLine = Color(0xFF90A4AE);
  static const bracketWinner = Color(0xFF00C853);
}
